# BeatSync Studio Backend Dependencies
# Python 3.10+ required

# Core dependencies
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.9
jsonschema>=4.21.0

# Audio analysis (from beatsync-core)
librosa>=0.10.1
numpy>=1.26.0
scipy>=1.11.0
soundfile>=0.12.1

# MIDI processing (from beatsync-midi)
mido>=1.3.0

# Local monorepo packages (installed via path in build step)
# beatsync-core and beatsync-midi are installed from sibling directories
# See render.yaml buildCommand for deployment install

# Development dependencies (optional)
pytest>=7.0.0
pytest-cov>=4.0.0
httpx>=0.26.0
pytest-playwright>=0.4.3
