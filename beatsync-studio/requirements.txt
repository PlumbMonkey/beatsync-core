# BeatSync Studio Backend Dependencies
# Python 3.10+ required
#
# DEPLOYMENT NOTE: On Render, dependencies are installed by setup.sh at the
# repo root via `pip install -e` for each local package.  This file is kept
# as a reference and for local venv setup only.
# Local dev: pip install -r beatsync-studio/requirements.txt
# Render:    bash setup.sh  (at repo root, installs everything)

# Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
jsonschema>=4.21.0

# Audio analysis — pulled in by beatsync-core via setup.sh on Render
# but listed here so local `pip install -r` works without setup.sh.
librosa>=0.10.1
# numpy 2.4.3 is pinned: required by scipy 1.17.1 (needs numpy.exceptions,
# unavailable in 1.x). Do NOT lower this to >=1.26.0.
numpy==2.4.3
scipy>=1.11.0
soundfile>=0.12.1

# MIDI processing — pulled in by beatsync-midi via setup.sh on Render
mido>=1.3.0
